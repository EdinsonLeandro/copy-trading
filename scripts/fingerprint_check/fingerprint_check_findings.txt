Fingerprint check findings - Binance scraper session config
Generated by: scripts/fingerprint_check/fingerprint_check.py
Date: 2026-09-23

CONTEXT
-------
Checked the exact browser/context setup used by every broker session in
this repo (src/common/browser_session.py: launch_browser() + a plain
new_context(viewport=None)) against two public fingerprint-check sites,
in response to Binance's Copy Trading leaderboard timing out mid-scrape
during phase 2 profile extraction.

RESULTS: bot.sannysoft.com
---------------------------
All checklist items passed (all green):
  - navigator.webdriver: false ("missing (passed)")
  - WebDriver Advanced: passed
  - Chrome (New): present (passed)
  - Plugins length / type: normal (5, PluginArray)
  - Languages: en-US, en
  - All PHANTOM_* / fingerprint-scanner rows: "ok"
  - Canvas hashes: consistent (expected within one browser)

No action needed here.

RESULTS: abrahamjuliot.github.io/creepjs
-----------------------------------------
- Headless/stealth scoring: 31% "like headless", 0% headless, 0% stealth-
  patched. Low risk - close to what a normal non-automated Chrome session
  scores.
- navigator.webdriver: false.

ISSUE FOUND (fixed): UA string vs. Client Hints/Worker mismatch
  - navigator.userAgent reported "Chrome/124.0.0.0" - this was a hardcoded
    DEFAULT_USER_AGENT constant in browser_session.py, pinned to an old
    Chrome version.
  - navigator.userAgentData (Client Hints) AND the Worker-thread context
    both reported the browser's REAL engine version: Chromium
    151.0.7922.34.
  - CreepJS flagged this itself (highlighted the Navigator and Worker
    sections as inconsistent/"lie").
  - Why this matters: a stale/spoofed UA string that disagrees with
    Client Hints is a cheap, high-confidence bot signal for any serious
    fingerprinting/anti-fraud vendor to check (Binance's leaderboard very
    likely sits behind one). This was probably a meaningful contributor
    to the observed throttling/timeouts.
  - FIX APPLIED: removed the hardcoded user_agent from both new_context()
    calls in browser_session.py. Playwright's default UA always matches
    the actual installed Chromium build across main thread, Workers, and
    Client Hints, so there's nothing to keep in sync going forward.
  - VERIFIED (2026-09-23 re-run): navigator.userAgent now reports
    "Chrome/151.0.0.0", matching navigator.userAgentData ("Chromium 151
    (151.0.7922.34)") and the Worker-context userAgent/userAgentData.
    CreepJS's Worker panel is no longer flagged/pink. Mismatch resolved.

MINOR / UNCONFIRMED
  - CreepJS's Screen section showed window outerHeight (964) larger than
    the reported screen height (720, from a 1280x720 display). This is
    a real inconsistency IF reproduced on the actual run machine, but
    this particular check was run inside a sandboxed dev environment
    with its own virtual display - so this may be an artifact of that
    sandbox, not of the user's real desktop. Re-run
    scripts/fingerprint_check.py on the actual machine that runs the
    scraper to confirm before treating this as a real issue.

NOT YET EVALUATED
  - playwright-stealth: not installed. Given the checklist items it
    targets (webdriver flag, plugins, permissions API, WebGL vendor
    strings, etc.) were already all passing before any stealth patching,
    installing it now would likely change nothing. Worth revisiting only
    if a future run of this same check shows a red/flagged item in that
    category.
  - TLS/JA3 fingerprinting, HTTP/2 frame ordering: out of reach of any
    Playwright-side JS patch; not checked by this tool.

HOW TO RE-RUN
-------------
    python scripts/fingerprint_check/fingerprint_check.py

Screenshots + raw navigator values are written to
scripts/fingerprint_check/fingerprint_check_results/ (sannysoft.png,
creepjs.png).
